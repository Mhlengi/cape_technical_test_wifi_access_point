[pytest]
DJANGO_SETTINGS_MODULE = cape_technical_test_wifi_access_point.settings
; Don't recreate the db on every test run
addopts = --reuse-db
norecursedirs =
    bin
    docs
    git_hooks
    venv
    .git
